@model E_Commerce_Mezzex.Models.Domain.Product

<div class="form-group mb-3">
    <label class="control-label mb-2">Up Sell Products</label>
    @if (ViewBag.UpsellProducts != null)
    {
        <div class="card-body">
            <div id="upSellProductsWrapper" class="dataTables_wrapper dt-bootstrap4">
                <div class="row">
                    <div class="col-sm-12">
                        <table id="upSellProductsTable" class="table table-bordered table-hover" aria-describedby="upSellProductsInfo">
                            <thead>
                                <tr>
                                    <th>Select</th>
                                    <th>Product Name</th>
                                    <th>Publish Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var product in ViewBag.UpsellProducts)
                                {
                                    <tr>
                                        <td>
                                            <div class="custom-control custom-checkbox">
                                                <input type="checkbox" class="custom-control-input" id="customCheckbox_@product.Value" name="UpsellProductIds" value="@product.Value" />
                                                <label class="custom-control-label" for="customCheckbox_@product.Value"></label>
                                            </div>
                                        </td>
                                        <td>@product.Text</td>
                                        <td>
                                            @if (product.Published)
                                            {
                                                <span class="publish-status text-success">✔</span>
                                            }
                                            else
                                            {
                                                <span class="publish-status text-danger">✘</span>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>Select</th>
                                    <th>Product Name</th>
                                    <th>Publish Status</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <p>No up sell products available</p>
    }
</div>
